# 开发环境配置
NEXT_PUBLIC_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000

# 应用配置
NEXT_PUBLIC_APP_NAME="i love delay (开发版)"

#zh en
NEXT_PUBLIC_DEFAULT_LOCALE=zh

# 数据库（开发/本地调试直连线上 dev 库）
# 推荐 DB 名称：i_love_delay_dev
# 示例：postgres://user:password@db-host:5432/i_love_delay_dev
DATABASE_URL=postgres://i_love_delay:123456@localhost:5432/i_love_delay_dev

# 若需 SSL：
# PGSSLMODE=require



# 部署变量（deploy.sh 会读取）

# 远程服务器信息
SERVER=49.235.209.193
SSH_USER=deployer
# 如果未使用 ssh-agent 或 ~/.ssh/config，请取消注释并填入私钥路径
SSH_KEY=~/.ssh/id_rsa

# 远程项目目录
REMOTE_DIR=/var/www/delay.bebackpacker.com

# 默认走远程部署 + 默认端口/程序名（这样就不必写 --port / --supervisor）
DEPLOY_REMOTE=true
DEPLOY_PORT=3002
DEPLOY_SUPERVISOR=delay.bebackpacker.com

# Supervisor 程序名（与 /etc/supervisor/conf.d/*.conf 中 program 名一致）
SUPERVISOR=delay.bebackpacker.com

# 服务器端口（如果你不用 3002，就这里改）
SERVER_PORT=3002
# 或用 PORT=3002（两者其一即可，SERVER_PORT 优先）